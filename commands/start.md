---
description: Read the active ticket's inputs/, analyze, ask clarifying questions, and finalize the scope. Alias of /vinsap:analyze and /vinsap:scope.
---

Invoke the `scope-builder` skill. See `commands/scope.md` for the full flow — this command is an alias of the same skill.
