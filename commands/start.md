---
description: Read the active ticket's intent + inputs/, ask clarifying questions, and finalize the spec. Alias of /vinsap:spec and /vinsap:analyze.
---

Invoke the `scope-builder` skill. See `commands/spec.md` for the full flow — this command is an alias of the same skill.
