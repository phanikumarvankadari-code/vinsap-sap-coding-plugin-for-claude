---
description: Generate code for the next (or specified) milestone, ABAP or Fiori path, with auto-generated tests. Legacy alias of /vinsap:build.
---

Invoke `abap-developer` or `fiori-developer` per the milestone's tag. See `commands/build.md` for the full flow — this command is a legacy alias, kept for backward compatibility with the pre-rename command name.
