---
description: Read inputs/, analyze, ask clarifying questions, and finalize the scope. Alias of /vinsap:scope and /vinsap:start.
---

Invoke the `scope-builder` skill. See `commands/scope.md` for the full flow — this command is an alias of the same skill.
