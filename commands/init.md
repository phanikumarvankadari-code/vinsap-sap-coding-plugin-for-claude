---
description: Start a new ticket (or, on the very first run for a project, walk through initial setup). Alias of /vinsap:intent.
---

Invoke the `onboarding-guide` skill. See `commands/intent.md` for the full flow — this command is an alias of the same skill.
