---
description: Break the active ticket's finalized spec into milestones, tagged ABAP/Fiori/mixed. Legacy alias of /vinsap:plan.
---

Invoke the `milestone-planner` skill. See `commands/plan.md` for the full flow — this command is a legacy alias, kept for backward compatibility with the pre-rename command name.
