# OS/editor noise
.DS_Store
*.swp
.vscode/

# Python (hooks/guard_sap_mcp.py)
__pycache__/
*.pyc

# Node (if any tooling scripts are added later)
node_modules/

# Local secrets/config — never commit real credentials
.env
*.local.json

# Project-instance state that belongs to a consuming project, not the plugin itself
/inputs/
/outputs/
/.sdlc/
